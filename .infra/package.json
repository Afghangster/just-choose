{
  "name": "just-choose-infra",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "synth": "npx cdk synth --profile org-admin",
    "deploy": "npx cdk deploy --profile org-admin",
    "diff": "npx cdk diff --profile org-admin",
    "destroy": "npx cdk destroy --profile org-admin"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.180.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "aws-cdk": "^2.180.0",
    "ts-node": "^10.9.2",
    "typescript": "~5.7.0"
  }
}
